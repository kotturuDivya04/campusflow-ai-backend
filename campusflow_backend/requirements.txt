# --- web framework -------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20          # multipart form parsing for timetable upload

# --- validation ----------------------------------------------------------
pydantic==2.10.4                  # Pydantic v2
email-validator==2.2.0

# --- database ------------------------------------------------------------
SQLAlchemy==2.0.36                # SQLAlchemy 2.0 style ORM
psycopg2-binary==2.9.10
alembic==1.14.0

# --- auth ----------------------------------------------------------------
PyJWT==2.10.1
passlib[bcrypt]==1.7.4            # verifies the bcrypt hashes in seed.sql
bcrypt==4.2.1

# --- tests ---------------------------------------------------------------
pytest==8.3.4
httpx==0.28.1                     # required by fastapi.testclient
